public enum OrboCoreBuild {
    /// Phase 0 linkage sentinel only.
    ///
    /// This exists to prove that Orbo and OrboLab can consume OrboCore.
    /// It carries no astrological or product semantics and may be removed once
    /// the native worksite connection is proven.
    public static let linkageSentinel = "0.0"
}
