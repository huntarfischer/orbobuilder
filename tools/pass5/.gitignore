# Native P22 completion workspace. Candidate/seal output is preserved locally until
# the sealed artifact is intentionally admitted into OrboCore shipping resources.
p22-build/

# Pass C OrboSpine manufacture workspace. Raw celestial/Terra/Ring/shell/eclipses
# remain local matter until Pass D compacts them into the runtime image.
orbospine-build/
