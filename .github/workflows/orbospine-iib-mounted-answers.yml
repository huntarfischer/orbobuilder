name: OrboSpine II-B Mounted Answers

on:
  push:
    branches:
      - feature/orbospine-shippable-iib-2026-09-03
  workflow_dispatch:

jobs:
  mounted-answers:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Mounted Spine answer proof
        working-directory: native/OrboCore
        run: swift test --filter OrboSpineArtifactMountedAnswerTests
