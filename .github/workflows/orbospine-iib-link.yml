name: OrboSpine II-B Link

on:
  push:
    branches:
      - feature/orbospine-shippable-iib-2026-09-03
  workflow_dispatch:

jobs:
  link:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Mounted Link identity proof
        working-directory: native/OrboCore
        run: swift test --filter OrboSpineArtifactLinkAnswerTests
