name: OrboSpine II-B Determinism

on:
  push:
    branches:
      - feature/orbospine-shippable-iib-2026-09-03
  workflow_dispatch:

jobs:
  determinism:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Deterministic artifact proof
        working-directory: native/OrboCore
        run: swift test --filter OrboSpineArtifactDeterminismTests
